spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/library_management?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&sslMode=DISABLED
    username: root
    password: 123456

jwt:
  secretKey: LibraryManagement2026SecretKeyForJWT!!  # 密钥（至少32字符）
  ttlMillis: 43200000  # 过期时间（毫秒）

file:
  upload:
    path: uploads  # 文件上传目录
